---
name: compare-branches
description: Compares two branches and lists the commits that only one of them has when the user asks for a diff.
argument-hint: '[base] [head]'
arguments:
  - branch
  - branch
---

# Compare branches

List the commits in $branch that the other branch lacks.
