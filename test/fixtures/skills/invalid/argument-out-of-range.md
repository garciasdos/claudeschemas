---
name: move-component
description: Moves a component to another directory and updates every import that referenced it.
argument-hint: '[component] [destination]'
arguments: component destination
---

# Move a component

1. Move $0 into $1.
2. Update the imports, then rename the test file to match $2.
