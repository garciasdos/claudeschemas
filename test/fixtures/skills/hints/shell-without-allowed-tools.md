---
name: run-tests
description: Runs the unit test suite and summarises the failures when the user asks whether the tests pass.
---

# Run tests

Run the suite:

```bash
npm test
```

Report the failing test names, one per line, and nothing else when everything passes.
